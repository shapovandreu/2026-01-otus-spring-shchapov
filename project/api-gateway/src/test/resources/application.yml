spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9/jwks

eureka:
  client:
    enabled: false
